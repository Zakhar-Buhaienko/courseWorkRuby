<!DOCTYPE html>
<html>
  <head>
    <title>Project</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>

    <!-- Додаємо Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <% unless controller_name == "sessions" && action_name == "new" %>
      <!-- Навігаційний хедер -->
      <header>
        <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #6b188c; padding: 20px 0;">
          <div class="container-fluid">
            <!-- Логотип або назва сайту -->
            <%= link_to 'Головна', root_path, class: 'navbar-brand' %>

            <!-- Кнопка для меню на мобільних -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Меню -->
            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                  <%= link_to 'Каталог', products_path, class: 'nav-link btn btn-outline-light mx-2 px-4 py-2 rounded-pill' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Корзина', cart_path, class: 'nav-link btn btn-outline-light mx-2 px-4 py-2 rounded-pill' %>
                </li>
                <li class="nav-item">
                  <%= link_to 'Профіль', edit_user_registration_path, class: 'nav-link btn btn-outline-light mx-2 px-4 py-2 rounded-pill' if user_signed_in? %>
                </li>
                <li class="nav-item">
                  <% if user_signed_in? %>
                    <%= link_to 'Вийти', destroy_user_session_path, method: :delete, class: 'nav-link btn btn-outline-light mx-2 px-4 py-2 rounded-pill', data: { confirm: 'Ви впевнені?' } %>
                  <% else %>
                    <%= link_to 'Увійти', new_user_session_path, class: 'nav-link btn btn-outline-light mx-2 px-4 py-2 rounded-pill' %>
                  <% end %>
                </li>
              </ul>
            </div>
          </div>
        </nav>
      </header>
    <% end %>

    <!-- Основний вміст сторінки -->
    <div class="container mt-4">
      <%= yield %>
    </div>

    <!-- Футер -->
    <footer class="bg-dark text-white py-3 mt-4">
      <div class="container text-center">
        <p>&copy; 2024 Мій сайт. Всі права захищені.</p>
      </div>
    </footer>

    <!-- JavaScript для Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
