<!DOCTYPE html>
<html lang="uk">
  <head>
    <title>Каталог продуктів</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>

    <!-- Додаємо Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Додаткові стилі для кнопок -->
    <style>
      .product-btn {
        border: 2px solid #6b188c; /* Білий колір обводки */
        background-color: transparent;
        color: #6b188c; /* Колір тексту */
        padding: 10px;
        border-radius: 5px;
        text-align: center;
        text-decoration: none;
        display: block;
        margin: 5px 0;
        font-size: 16px;
        transition: background-color 0.3s ease, color 0.3s ease;
      }

      .product-btn:hover {
        background-color: #6b188c; /* Колір фону при наведенні */
        color: white; /* Колір тексту при наведенні */
      }

      .product-btn:active {
        background-color: #8918db; /* Повернення до іншого фону при натисканні */
        color: white;
      }

      .product-btn-container {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }

      .add-product-btn {
        border: 2px solid #8918db;
        color: #8918db;
      }

      .add-product-btn:hover {
        background-color: #8918db;
        color: white;
      }

    </style>
  </head>

  <body style="background-color: #daf4ce; color: #1f196c;">
    <div class="container mt-4">
      <h1 class="text-center mb-4" style="color: #6b188c;">Список товарів</h1>

      <!-- Сітка товарів -->
      <div class="row g-3">
        <% @products.each do |product| %>
          <div class="col-md-4 col-lg-3">
            <div class="card shadow-sm" style="background-color: #fff;">
              <%= image_tag product.image, alt: product.name, class: 'card-img-top', style: 'height: 200px; object-fit: cover;' if product.image.present? %>
              <div class="card-body">
                <h5 class="card-title" style="color: #6b188c;"><%= product.name %></h5>
                <p class="card-text" style="color: #8918db;">Ціна: <%= number_to_currency(product.price) %></p>
                <p class="card-text">Кількість: <%= product.stock %></p>

                <!-- Кнопки одна під одною -->
                <div class="product-btn-container">
                  <%= link_to 'Переглянути', product_path(product), class: 'product-btn' %>
                  <% if user_signed_in? && current_user.admin? %>
                    <%= link_to 'Редагувати', edit_product_path(product), class: 'product-btn' %>
                    <%= button_to 'Видалити', product_path(product), method: :delete, data: { confirm: 'Ви впевнені?' }, class: 'product-btn' %>
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
      <% if user_signed_in? && current_user.admin? %>
        <!-- Кнопка для додавання товару в кінці -->
        <div class="d-flex justify-content-center mt-4">
          <%= link_to 'Додати товар', new_product_path, class: 'product-btn add-product-btn' %>
        </div>
      <% end %>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
